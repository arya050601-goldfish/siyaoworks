# AGENTS.md — siyaoworks.com

本文件适用于整个仓库。它记录的是改版与发布硬门槛，不是视觉建议。

## 核心判断：访客只给 10 秒

默认访客第一次打开网站，只会给页面 10 秒。

首页必须在这 10 秒里让人知道：

1. 这是谁；
2. 她主要帮哪类人；
3. 她能解决什么具体问题；
4. 最可信的案例或证据在哪里；
5. 下一步可以看案例，还是加微信聊。

案例页必须在这 10 秒里让人知道：项目解决什么问题、金鱼负责什么、项目进行到哪里、有哪些可见证据。

任何一项需要访客猜、反复找或先读完大段文字，改版就不合格，不得上线。

每次视觉或信息结构改动后，必须分别截取 `1440×900` 和 `390×844` 的入口屏。把截图交给至少一名没有参与本次改动的 reviewer / agent，只看 10 秒，不补充解释，然后回答上面五个问题。这只能叫“10 秒代理盲测”：它可以暴露信息是否显眼和代理误判点，不能证明目标用户已经理解。首页或定位发生大改时，再用 3—5 名接近目标客群的人做真实测试。测试原话和误判点要留下，不能由改版者替对方总结成正确答案。

## 动手前先冻结范围

每次修改前先写清：要改哪些页面、区块和断点，手机或桌面是否冻结，哪些事实和状态可以改，哪些文件不碰。桌面专属改动也可能改变手机 DOM 高度；不能把“只改桌面”当成免测手机的理由。

如果某一端明确冻结，不得出现未授权的可见位移、断行、顺序或交互变化。可用同一浏览器环境下关键区块 `top` / `height` 变化超过 `8px` 作为人工复核预警，但不能只凭像素差判失败。若本轮明确新增目录或入口，必须记录首个强视觉证据的位置和整页高度的前后变化；后移超过一个该端视口或页面增长超过 15% 时，必须单独说明影响并取得确认，而不是自动把它当成合理变化。

## 视觉硬门槛

### 1. 标题先负责导航，再负责气势

- 普通内容区和案例区标题，桌面端默认采用 `40—48px`，移动端不得超过 `34px`。桌面宽栏标题只有在不超过 `54px`、不超过 2 行、同屏仍有内容或证据且代理盲测通过时，才允许例外；超过 `54px` 直接失败。
- 首页展示型标题只有在说明文字、主要证据和首要行动仍能进入首屏时，才允许更大。
- 一个标题最多 2—3 行。不得把一个完整中文词拆成“杯 / 子”“执 / 行”“验 / 收”，也不得留下单字孤行。
- 字号必须跟实际文字列宽匹配。禁止让 `vw` 在已封顶的内容容器里继续放大。
- 标题不能独占一屏。访客必须同时看见解释、证据或下一步，而不是只看见一句口号。
- 普通桌面标题整体高度不超过 `180px`，移动端不超过 `140px`；展示型首屏标题不超过首屏高度的 40%。
- 使用 `nowrap`、显式断行或嵌套 `span` 时，子元素的 `scrollWidth` 不得超过自身 `clientWidth + 1px`。不能靠溢出到栏间空隙里“刚好没撞上图片”。

### 2. 一屏只有一个主视觉中心

- 每个模块都要有明确的标签、标题和核心内容；主要入口模块必须有清楚的下一步。纯摘要要明确标注“本页摘要 / 无独立页”，不强制按钮，也不能让访客猜模块在说什么。
- 模块之间至少用留白、背景、边框或完整卡片中的一种拉开边界。
- 相邻案例至少同时使用两种区隔信号：例如编号或状态，加上边框、底色或不少于 `16px` 的明显间距。
- 不同项目不能做成同权长文。完整案例、未实施方案、待验证方案和本页摘要必须在视觉与文字上区分。
- 首页案例统一按“项目编号 / 项目名 / 当前状态 / 卡住的问题 / 做了什么 / 证据或证据边界 / 去向（如有）”组织。
- 只有真正可进入的内容才做成可点击样式；不可点击的摘要不画假入口。

### 3. 项目状态和证据必须说实话

- 固定区分“已完成客户项目”“已完成客户支持”“受邀继续参与”“独立方案 · 未实施”“独立诊断方案 · 待验证”“产品判断记录”。
- 同一项目在索引、摘要、案例页、页面标题和分享文案中的状态必须一致。
- 做过诊断不等于方案实施；交付方案不等于对方采用；培训和配置完成不等于团队持续使用；再次邀请不等于经营结果。
- 数字只能说明它真实能说明的事。`10 张主图、9 屏详情页`是交付数量，不是销量或转化证据。
- 本站自己的项目摘要不能被写成独立证据；“可看成果”必须真的有页面、文件或成品可看。

### 4. 证据图不能被当成填充物

- 案例里的真实成品图和过程图可以作为证据；示意图、装饰图和 AI 生成图必须标明性质，不得冒充真实项目证据。
- 重要图片中的产品、标题、说明文字和品牌标识不得被裁掉。
- 优先保留原始比例；必须裁切时，先确认裁切不会改变信息。
- 图注必须在浅色、深色和断点切换后都可读，不能出现白底白字或文字落在画面外。
- 移动端可以减少拼贴和装饰，但不能把最强的视觉证据推到几屏以后。

## 每次改版必须做的回归

只改桌面端，也要复查移动端；只改移动端，也要复查桌面端。

至少检查这些视口：

- 移动端：`320×800`、`375×812`、`390×844`、`430×932`
- 关键边界：`700×900`、`701×900`、`960×900`、`961×900`
- 桌面端：`1024×768`、`1280×800`、`1440×900`、`1920×1080`

每个视口都要检查：

- 五项关键信息是否可见、可辨；只有目标客群真实测试才能下“访客已经理解”的结论；
- 标题字号、语义断行和行数；
- 模块边界、阅读顺序和唯一主视觉；
- 案例身份、状态、证据强弱和入口是否清楚；
- 图片是否完整，图注是否可读；
- 是否有横向滚动、遮挡、空白卡尾或内容跳位；
- 导航、锚点、按钮和微信入口是否能用；
- 是否有资源 404、重复 ID、缺失锚点或控制台错误。

另做一次桌面端 `125%` 和 `150%` 浏览器缩放压力测试。锚点滚动完成后，目标标题应落在固定导航下方，而不是被遮住。主要链接必须能用键盘到达；减少动画偏好开启后，页面仍须可用。

## 发布拦截规则

以下任一情况出现，停止发布并先修复：

- 首屏只剩大标题，访客看不见解释、证据或动作；
- 标题出现不自然断词、单字孤行，或占据大半屏；
- 相邻案例看起来像同一段内容，无法快速区分；
- 项目状态或证据等级会让人误以为“已经实施”或“已经产生结果”；
- 关键图片、图片内文字或品牌标识被裁掉；
- 任一必测视口出现横向溢出、不可读图注或错位；
- 页面根节点没有横向滚动，但标题子元素已经溢出自己的文字列；
- 只验证本地页面，没有验证发布后的 `https://siyaoworks.com/`。

## 发布证据

上线前必须留下：

1. 本次测试过的视口清单；
2. 至少一张移动端和一张桌面端最终截图；
3. 链接、资源、锚点、控制台和横向溢出的检查结果；
4. 发布 commit；
5. 线上域名的读回与截图结果。

发布后至少重新验证线上 `390×844`、`1024×768` 和 `1440×900`。确认线上 HTML 与样式版本属于本次 commit，关键案例页和锚点能打开，不能只看到部署成功就结束。

提交前只显式暂存本轮冻结清单中的文件，禁止使用 `git add .` 或 `git add -A`。执行 `git diff --cached --name-only`，结果必须与冻结清单一致。当前 `.stfolder/` 与 `ai-speech-ppt-interview-supplement.md` 不属于网站改版提交，除非用户另行授权。

发布只使用仓库根目录与 `CNAME`。`dist/` 和 `siyaoworks-site-dist.zip` 是旧产物，不得作为线上源。

没有这些证据，不得说“已经检查过”或“可以发布”。
